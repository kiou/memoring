<nav class="navigation">
    <div class="inner">
        
        {% if action is defined %}
            <div class="navLeft">
                <a href="#" class="linknav" data-nav=".section3">Question</a>
            </div>
        {% endif %}
        
        <div class="navCentre {% if action is not defined %}full{% endif %}">
            <div class="logo">
                {% if action is defined %}
                    <img src="{{ asset('img/logomenu.png') }}" class="linknav" data-nav=".content" alt="L'informatique avec vous logo">
                {% else %}
                    <a href="{{ path('app_index') }}"><img src="{{ asset('img/logomenu.png') }}" alt="L'informatique avec vous logo"></a>
                {% endif %}
            </div>
        </div> 

        {% if action is defined %}
            <div class="navRight">
                <a href="#" class="linknav" data-nav=".section5">Strategie</a>
            </div>
        {% endif %}
        
        {% if action is defined %}
            <div class="navigationrightmobile">
                <i class="fas fa-bars"></i>
            </div>

            <div class="mobilemenucontent">
                <a href="">Lien</a>
            </div>
        {% endif %}

        <div class="clear"></div>
    </div>
</nav>